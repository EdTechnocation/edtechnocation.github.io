<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ctrl+AI: Vibe Coding & the Future of K-12 CS</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- React Libraries -->
    <script src="https://unpkg.com/react@17/umd/react.development.js" crossorigin></script>
    <script src="https://unpkg.com/react-dom@17/umd/react-dom.development.js" crossorigin></script>
    <script src="https://unpkg.com/babel-standalone@6/babel.min.js"></script>
    <style>
        /* Custom font and base styles */
        body {
            font-family: 'Inter', sans-serif;
        }
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap');

        /* Background grid pattern */
        .bg-grid-gray-700\/\[0\.2\] {
            background-image: linear-gradient(to right, rgba(107, 114, 128, 0.1) 1px, transparent 1px), linear-gradient(to bottom, rgba(107, 114, 128, 0.1) 1px, transparent 1px);
            background-size: 2rem 2rem;
        }
        
        /* Fade-in animation for slides */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate-fade-in {
            animation: fadeIn 0.5s ease-in-out forwards;
        }

        /* Hide all slides by default */
        .slide {
            display: none;
            width: 100%;
            height: 100%;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }
        
        /* Show the active slide */
        .slide.active {
            display: flex;
        }
        
        /* Loading spinner animation */
        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        .animate-spin {
            animation: spin 1s linear infinite;
        }
    </style>
</head>
<body class="bg-gray-900 text-white font-sans w-full h-screen flex flex-col justify-center items-center p-4 relative overflow-hidden">
    
    <!-- Background decorations -->
    <div class="absolute inset-0 bg-grid-gray-700/[0.2] z-0"></div>
    <div class="absolute inset-0 bg-gradient-to-t from-gray-900 via-gray-900/80 to-transparent z-0"></div>
    
    <!-- Main presentation container -->
    <div id="presentation-container" class="w-full h-full max-w-5xl max-h-[600px] bg-gray-800 rounded-2xl shadow-2xl shadow-cyan-500/10 border border-gray-700 flex flex-col justify-center items-center p-8 md:p-16 relative z-10">

        <!-- Slide 1: Title -->
        <div class="slide active text-center">
            <div class="flex items-center justify-center bg-cyan-500/10 p-4 rounded-full mb-6 border border-cyan-500/20">
                <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-cyan-400"><path d="M15 6v12a3 3 0 1 0 3-3H6a3 3 0 1 0 3 3V6a3 3 0 1 0-3 3h12a3 3 0 1 0-3-3z"/></svg>
            </div>
            <h1 class="text-5xl md:text-6xl font-bold tracking-tight bg-clip-text text-transparent bg-gradient-to-br from-white to-gray-400">Ctrl+AI</h1>
            <h2 class="text-2xl md:text-3xl text-gray-300 mt-2">Vibe Coding & the Future of K-12 CS</h2>
            <p class="mt-8 text-lg text-gray-400">Michael Fricano II | K-6 Design and Technology Teacher & EdTech Enthusiast</p>
            <a href="http://www.edtechnocation.com/vibecoding" target="_blank" class="mt-4 text-2xl text-cyan-400 hover:underline">http://www.edtechnocation.com/vibecoding</a>
        </div>

        <!-- Slide 2: Core Question -->
        <div class="slide text-center">
            <p class="text-xl md:text-2xl text-cyan-400 mb-4">"Vibe coding" allows users to create with AI using natural language.</p>
            <h1 class="text-3xl md:text-5xl font-bold text-white leading-tight">What does this mean for K-12 Computer Science?</h1>
            <div class="mt-8 flex justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" width="80" height="80" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-gray-500"><path d="M12 2a10 10 0 0 0-2 19.5v-2.5a7.5 7.5 0 0 1 0-15V2A10 10 0 0 0 12 2Z"/><path d="M10 12.5a2.5 2.5 0 1 0-5 0 2.5 2.5 0 0 0 5 0Z"/><path d="M15 12.5a2.5 2.5 0 1 0-5 0 2.5 2.5 0 0 0 5 0Z"/><path d="M12.5 10a2.5 2.5 0 1 0 0-5 2.5 2.5 0 0 0 0 5Z"/><path d="M12.5 15a2.5 2.5 0 1 0 0 5 2.5 2.5 0 0 0 0-5Z"/><path d="M10 10a2.5 2.5 0 1 0 0-5 2.5 2.5 0 0 0 0 5Z"/><path d="M15 15a2.5 2.5 0 1 0 0 5 2.5 2.5 0 0 0 0 5Z"/></svg>
            </div>
        </div>
        
        <!-- Slide 3: Agenda -->
        <div class="slide text-left">
            <h1 class="text-4xl font-bold text-cyan-400 mb-8 text-center">Today's Agenda</h1>
            <ul class="space-y-4 text-xl text-gray-300">
                <li class="flex items-center"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-cyan-400 mr-4"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path></svg> What is "Vibe Coding"?</li>
                <li class="flex items-center"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-cyan-400 mr-4"><path d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"/><path d="M14 2v6h6"/><path d="M9 14h.01"/><path d="M15 14h.01"/><path d="M12 18h.01"/></svg> AI in Action: Live Demos</li>
                <li class="flex items-center"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-cyan-400 mr-4"><path d="m16 16 3-8 3 8c-.87.65-1.92 1-3 1s-2.13-.35-3-1Z"/><path d="m2 16 3-8 3 8c-.87.65-1.92 1-3 1s-2.13-.35-3-1Z"/><path d="M12 22V6"/><path d="M6 6h12"/><path d="M12 6V2"/></svg> Practical & Ethical Classroom Use</li>
                <li class="flex items-center"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-cyan-400 mr-4"><path d="M12 2a10 10 0 0 0-2 19.5v-2.5a7.5 7.5 0 0 1 0-15V2A10 10 0 0 0 12 2Z"/><path d="M10 12.5a2.5 2.5 0 1 0-5 0 2.5 2.5 0 0 0 5 0Z"/><path d="M15 12.5a2.5 2.5 0 1 0-5 0 2.5 2.5 0 0 0 5 0Z"/><path d="M12.5 10a2.5 2.5 0 1 0 0-5 2.5 2.5 0 0 0 0 5Z"/><path d="M12.5 15a2.5 2.5 0 1 0 0 5 2.5 2.5 0 0 0 0-5Z"/><path d="M10 10a2.5 2.5 0 1 0 0-5 2.5 2.5 0 0 0 0 5Z"/><path d="M15 15a2.5 2.5 0 1 0 0 5 2.5 2.5 0 0 0 0 5Z"/></svg> Augmenting, Not Replacing, Critical Thinking</li>
                <li class="flex items-center"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-cyan-400 mr-4"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg> Q&A and Discussion</li>
            </ul>
        </div>

        <!-- Slide 4: What is Vibe Coding? -->
        <div class="slide text-center">
            <h1 class="text-4xl font-bold text-cyan-400 mb-6">What is "Vibe Coding"?</h1>
            <p class="text-xl text-gray-300 mb-8">Instructing AI with natural language to generate, refine, and debug code.</p>
            <div class="grid md:grid-cols-2 gap-8 items-center text-left">
                <div class="bg-gray-700/50 p-6 rounded-lg border border-gray-600">
                    <h3 class="text-lg font-semibold text-white mb-2">The Analogy</h3>
                    <p class="text-gray-400">You are the <span class="text-cyan-400 font-bold">Creative Director</span>.</p>
                    <p class="text-gray-400">The AI is the <span class="text-cyan-400 font-bold">Apprentice Coder</span>.</p>
                </div>
                <div class="bg-gray-700/50 p-6 rounded-lg border border-gray-600">
                    <h3 class="text-lg font-semibold text-white mb-2">The Focus</h3>
                    <p class="text-gray-400">Focus shifts from <span class="line-through">syntax</span> to <span class="text-cyan-400 font-bold">intent</span>.</p>
                </div>
            </div>
        </div>

        <!-- Slide 5: Pedagogical Shift -->
        <div class="slide text-center">
            <h1 class="text-4xl font-bold text-cyan-400 mb-6">From Syntax Memorizers to System Thinkers</h1>
            <p class="text-xl text-gray-300 mb-8">We are teaching students how to be architects, not just bricklayers.</p>
            <div class="flex flex-col md:flex-row justify-around gap-6">
                <div class="flex-1 bg-red-500/10 p-6 rounded-lg border border-red-500/20 text-center">
                    <h3 class="text-2xl font-bold text-red-300 mb-2">Old Focus</h3>
                    <ul class="text-gray-300 space-y-2">
                        <li>Memorizing syntax</li>
                        <li>Line-by-line logic</li>
                    </ul>
                </div>
                <div class="flex items-center justify-center text-cyan-400">
                    <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="hidden md:block"><path d="m9 18 6-6-6-6"></path></svg>
                </div>
                <div class="flex-1 bg-green-500/10 p-6 rounded-lg border border-green-500/20 text-center">
                    <h3 class="text-2xl font-bold text-green-300 mb-2">New Focus</h3>
                    <ul class="text-gray-300 space-y-2">
                        <li>Problem Decomposition</li>
                        <li>Systems Thinking</li>
                        <li>Critical Evaluation</li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- Slide 6: Interactive Poll -->
        <div class="slide text-center">
            <h1 class="text-4xl font-bold text-cyan-400 mb-4">Let's Hear From You</h1>
            <p class="text-xl text-gray-300 mb-8">What is your biggest concern regarding students using AI to write code?</p>
            <div class="mt-8 text-center">
                <p class="text-2xl text-gray-300">Go to:</p>
                <a href="https://www.menti.com" target="_blank" class="text-4xl font-bold text-cyan-400 hover:underline">www.menti.com</a>
                <p class="mt-6 text-2xl text-gray-300">And enter the code:</p>
                <p class="mt-2 text-5xl font-bold tracking-widest text-white bg-gray-700/50 px-6 py-3 rounded-lg inline-block">6660 0938</p>
            </div>
        </div>

        <!-- Slide 7: Disclaimer -->
        <div class="slide text-center">
            <h1 class="text-4xl font-bold text-cyan-400 mb-6">A Quick Disclaimer</h1>
            <div class="bg-gray-700/50 p-8 rounded-lg border border-gray-600">
                <p class="text-xl text-gray-300 leading-relaxed">As we demo these tools, let's wear two hats: one of a <span class="text-cyan-300">student creator</span>, and one of a <span class="text-cyan-300">teacher assessor</span>.</p>
                <p class="mt-4 text-lg text-gray-400">The goal is to understand the <span class="font-bold">potential</span>, not master a specific tool.</p>
            </div>
        </div>
        
        <!-- Slide 8: Gemini Demo 1 -->
        <div class="slide text-left">
            <div class="w-full h-full flex flex-col">
                <h1 class="text-4xl font-bold text-cyan-400 mb-2">Demo 1: ✨ Vibe Coding Demo</h1>
                <p class="text-lg text-gray-400 mb-4">Let's generate a simple guessing game.</p>
                <div class="flex-grow flex flex-col gap-4 min-h-0">
                    <textarea id="demo1-prompt" class="w-full p-2 bg-gray-900 rounded-lg font-mono text-sm text-gray-300 border border-gray-700 focus:ring-2 focus:ring-cyan-500 focus:outline-none" rows="3" placeholder="Enter your 'vibe' prompt here...">The computer thinks of a number from 1-20. It asks for a guess and tells me if I'm too high, too low, or correct.</textarea>
                    <button id="demo1-button" class="w-full flex justify-center items-center gap-2 px-4 py-2 bg-cyan-600 text-white font-bold rounded-lg hover:bg-cyan-500 transition-colors disabled:bg-gray-600 disabled:cursor-not-allowed">
                        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m5 3 7 5.5L19 3"/><path d="m5 21 7-5.5L19 21"/><path d="M12 22V8"/></svg>
                        <span class="button-text">Generate Code</span>
                    </button>
                    <div class="flex-grow w-full bg-gray-900/80 rounded-lg border border-gray-700 p-4 overflow-auto min-h-0">
                        <pre class="whitespace-pre-wrap text-sm"><code id="demo1-output" class="language-python"><span class="text-gray-500">Generated code will appear here.</span></code></pre>
                    </div>
                </div>
            </div>
        </div>

        <!-- Slide 9: Interactive Game -->
        <div class="slide text-center">
             <div class="w-full h-full flex flex-col items-center justify-center">
                <h1 class="text-4xl font-bold text-cyan-400 mb-4">Try It Out: Guess the Number!</h1>
                <div id="game-container" class="w-full max-w-sm p-6 bg-gray-700/50 rounded-lg border border-gray-600">
                    <p id="game-message" class="text-lg text-center h-12 mb-4">I'm thinking of a number between 1 and 20.</p>
                    <input type="number" id="game-input" class="w-full p-3 text-center text-xl bg-gray-900 rounded-lg border border-gray-500 focus:ring-2 focus:ring-cyan-500 focus:outline-none" placeholder="Enter your guess...">
                    <button id="game-guess-btn" class="w-full mt-4 px-4 py-3 bg-cyan-600 text-white font-bold rounded-lg hover:bg-cyan-500 transition-colors">Guess</button>
                    <button id="game-reset-btn" class="w-full mt-2 px-4 py-2 bg-gray-600 text-white font-bold rounded-lg hover:bg-gray-500 transition-colors">New Game</button>
                </div>
            </div>
        </div>

        <!-- Slide 10: Demo 2a - Debugging -->
        <div class="slide text-left">
            <h1 class="text-4xl font-bold text-cyan-400 mb-4">Demo 2a: AI for Debugging</h1>
            <p class="text-lg text-gray-400 mb-6">Using AI to find and fix errors in code.</p>
            <div class="bg-gray-900 p-4 rounded-lg font-mono text-sm text-gray-300 border border-gray-700 mb-4">
                <p><span class="text-cyan-400">Debugging Prompt:</span></p>
                <p class="mt-2">"Find the bug in this code."</p>
            </div>
            <div class="w-full h-48 bg-gray-900 rounded-lg border border-gray-700 p-4 overflow-auto">
                <pre class="whitespace-pre-wrap text-sm"><code class="language-python">
import random

secret_number = random.randint(1, 20)
player_guess = 0

print("Hello! I'm thinking of a number between 1 and 20.")
print("Can you guess what it is?")while player_guess != secret_number:
    try:
        player_guess = int(input("Enter your guess: )
    except ValueError:
        print("That's not a valid number. Please enter a whole number.")
        continue

    if player_guess < secret_number:
        print("Your guess is too low! Try again.")
    elif player_guess > secret_number:
        print("Your guess is too high! Try again.")
    else:
        print(f"Congratulations! You guessed it! The number was {secret_number}.")

print("Thanks for playing!")
                </code></pre>
            </div>
        </div>

        <!-- Slide 11: Demo 2b - Explanation -->
        <div class="slide text-left">
            <h1 class="text-4xl font-bold text-cyan-400 mb-4">Demo 2b: AI for Explanation</h1>
            <p class="text-lg text-gray-400 mb-6">Using AI to understand complex code.</p>
            <div class="bg-gray-900 p-4 rounded-lg font-mono text-sm text-gray-300 border border-gray-700 mb-4">
                <p><span class="text-cyan-400">Explanation Prompt:</span></p>
                <p class="mt-2">"Explain this React code for a bowling scorer to me like I'm a 10th grader."</p>
            </div>
            <div class="w-full h-48 bg-gray-900 rounded-lg border border-gray-700 p-4 overflow-auto">
                <pre class="whitespace-pre-wrap text-xs"><code class="language-jsx">
import React, { useState, useEffect } from 'react';

// --- Helper Functions & Initial State ---
const initialFrames = () => Array(10).fill(null).map(() => ({ rolls: [], score: null, display: [] }));
const isStrike = (frame) => frame && frame.rolls[0] === 10;
const isSpare = (frame) => frame && frame.rolls.length === 2 && frame.rolls[0] + frame.rolls[1] === 10;

// --- Main App Component ---
const App = () => {
    const [frames, setFrames] = useState(initialFrames());
    const [currentFrameIndex, setCurrentFrameIndex] = useState(0);
    const [pinsRemaining, setPinsRemaining] = useState(10);
    const [gameOver, setGameOver] = useState(false);
    const [cumulativeScores, setCumulativeScores] = useState(Array(10).fill(null));

    // --- Score Calculation Logic ---
    const calculateScores = (currentFrames) => {
        let runningTotal = 0;
        const newCumulativeScores = Array(10).fill(null);
        for (let i = 0; i < 10; i++) {
            const frame = currentFrames[i];
            if (frame === null || frame.rolls.length === 0) break;
            let frameScore = 0;
            if (i < 9) { // Frames 1-9
                if (isStrike(frame)) {
                    const nextFrame = currentFrames[i + 1];
                    const nextNextFrame = currentFrames[i + 2];
                    if (nextFrame && nextFrame.rolls.length > 0) {
                        if (isStrike(nextFrame)) {
                            if (nextNextFrame && nextNextFrame.rolls.length > 0) {
                                frameScore = 10 + 10 + nextNextFrame.rolls[0];
                            } else { break; }
                        } else if (nextFrame.rolls.length === 2) {
                            frameScore = 10 + nextFrame.rolls[0] + nextFrame.rolls[1];
                        } else { break; }
                    } else { break; }
                } else if (isSpare(frame)) {
                    const nextFrame = currentFrames[i + 1];
                    if (nextFrame && nextFrame.rolls.length > 0) {
                        frameScore = 10 + nextFrame.rolls[0];
                    } else { break; }
                } else {
                    if (frame.rolls.length === 2) {
                        frameScore = frame.rolls[0] + frame.rolls[1];
                    } else { break; }
                }
            } else { // 10th Frame
                frameScore = frame.rolls.reduce((sum, roll) => sum + roll, 0);
            }
            if(frameScore !== null) {
                runningTotal += frameScore;
                newCumulativeScores[i] = runningTotal;
            }
        }
        setCumulativeScores(newCumulativeScores);
    };
    
    useEffect(() => {
        calculateScores(frames);
    }, [frames]);

    // --- Game Logic ---
    const handleRoll = (pins) => {
        if (gameOver) return;
        const newFrames = [...frames];
        let currentFrame = newFrames[currentFrameIndex];
        if (!currentFrame) {
            currentFrame = { rolls: [], score: null, display: [] };
            newFrames[currentFrameIndex] = currentFrame;
        }
        currentFrame.rolls.push(pins);
        
        // --- Display Logic (X, /, -) ---
        if (currentFrameIndex < 9) {
            if (currentFrame.rolls.length === 1 && pins === 10) { // Strike
                currentFrame.display = ['', 'X'];
            } else if (currentFrame.rolls.length === 2 && pins + currentFrame.rolls[0] === 10) { // Spare
                currentFrame.display = [currentFrame.rolls[0], '/'];
            } else {
                currentFrame.display = [...currentFrame.rolls];
            }
        } else { // 10th Frame display logic
            currentFrame.display = currentFrame.rolls.map(roll => {
                if (roll === 10) return 'X';
                if (currentFrame.rolls.length >= 2 && currentFrame.rolls[0] + roll === 10 && currentFrame.display.length === 1) return '/';
                return roll;
            });
        }

        setFrames(newFrames);
        updateGameState(pins);
        calculateScores(newFrames);
    };

    const updateGameState = (pins) => {
        const isTenthFrame = currentFrameIndex === 9;
        const currentFrame = frames[currentFrameIndex];
        if (isTenthFrame) {
            const [firstRoll, secondRoll] = currentFrame.rolls;
            const isStrikeOrSpare = (firstRoll === 10 || firstRoll + secondRoll === 10);
            if (currentFrame.rolls.length === 3 || (currentFrame.rolls.length === 2 && !isStrikeOrSpare)) {
                setGameOver(true);
            } else {
                if (pins === 10 || (currentFrame.rolls.length === 2 && firstRoll + pins === 10)) {
                    setPinsRemaining(10); // Reset for fill ball
                } else {
                    setPinsRemaining(10 - pins);
                }
            }
        } else {
            if (pins === 10 || currentFrame.rolls.length === 2) {
                setCurrentFrameIndex(currentFrameIndex + 1);
                setPinsRemaining(10);
            } else {
                setPinsRemaining(10 - pins);
            }
        }
    };
    
    const newGame = () => {
        setFrames(initialFrames());
        setCurrentFrameIndex(0);
        setPinsRemaining(10);
        setGameOver(false);
        setCumulativeScores(Array(10).fill(null));
    };

    return ( 
        // JSX for UI components goes here...
    );
};
                </code></pre>
            </div>
        </div>

        <!-- NEW Slide 12: Interactive Bowling App -->
        <div class="slide text-center">
            <div id="bowling-app-root" class="w-full h-full bg-gray-900 text-white flex flex-col items-center justify-center p-4 font-sans scale-90"></div>
        </div>

        <!-- Slide 13: Integrity Checkpoint -->
        <div class="slide text-center">
            <svg xmlns="http://www.w3.org/2000/svg" width="60" height="60" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-cyan-400 mx-auto mb-6"><path d="m16 16 3-8 3 8c-.87.65-1.92 1-3 1s-2.13-.35-3-1Z"/><path d="m2 16 3-8 3 8c-.87.65-1.92 1-3 1s-2.13-.35-3-1Z"/><path d="M12 22V6"/><path d="M6 6h12"/><path d="M12 6V2"/></svg>
            <h1 class="text-4xl font-bold text-white mb-4">How do we assess learning?</h1>
            <p class="text-2xl text-gray-300">How can we maintain academic integrity when the code isn't 100% student-written?</p>
        </div>

        <!-- Slide 14: Augmenting Not Replacing -->
        <div class="slide text-center">
            <h1 class="text-4xl font-bold text-cyan-400 mb-6">The Teacher's Evolving Role</h1>
            <p class="text-xl text-gray-300 mb-8">The teacher becomes a...</p>
            <div class="flex flex-col md:flex-row justify-around gap-6 text-white">
                <div class="flex-1 text-center">
                    <div class="bg-gray-700/50 w-24 h-24 rounded-full mx-auto flex items-center justify-center border-2 border-cyan-500/50"><svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg></div>
                    <h3 class="text-2xl font-bold mt-4">Guide</h3>
                </div>
                <div class="flex-1 text-center">
                    <div class="bg-gray-700/50 w-24 h-24 rounded-full mx-auto flex items-center justify-center border-2 border-cyan-500/50"><svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="22" y1="2" x2="11" y2="13"></line><polygon points="22 2 15 22 11 13 2 9 22 2"></polygon></svg></div>
                    <h3 class="text-2xl font-bold mt-4">Project Manager</h3>
                </div>
                <div class="flex-1 text-center">
                    <div class="bg-gray-700/50 w-24 h-24 rounded-full mx-auto flex items-center justify-center border-2 border-cyan-500/50"><svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m16 16 3-8 3 8c-.87.65-1.92 1-3 1s-2.13-.35-3-1Z"/><path d="m2 16 3-8 3 8c-.87.65-1.92 1-3 1s-2.13-.35-3-1Z"/><path d="M12 22V6"/><path d="M6 6h12"/><path d="M12 6V2"/></svg></div>
                    <h3 class="text-2xl font-bold mt-4">Ethical Compass</h3>
                </div>
            </div>
            <p class="mt-8 text-xl text-gray-300">Focus assessment on the <span class="text-cyan-400 font-bold">process</span>, not just the final product.</p>
        </div>

        <!-- Slide 15: Academic Integrity Strategies -->
        <div class="slide text-left">
            <h1 class="text-4xl font-bold text-cyan-400 mb-8 text-center">Strategies for Academic Integrity</h1>
            <ul class="space-y-4 text-xl text-gray-300">
                <li class="flex items-start"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-green-400 mr-4 mt-1 flex-shrink-0"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path><polyline points="22 4 12 14.01 9 11.01"></polyline></svg> <span><span class="font-bold text-white">"Show Your Work":</span> Students submit their AI prompt history.</span></li>
                <li class="flex items-start"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-green-400 mr-4 mt-1 flex-shrink-0"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path><polyline points="22 4 12 14.01 9 11.01"></polyline></svg> <span><span class="font-bold text-white">Code Reviews:</span> Students must explain and defend the AI's code.</span></li>
                <li class="flex items-start"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-green-400 mr-4 mt-1 flex-shrink-0"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path><polyline points="22 4 12 14.01 9 11.01"></polyline></svg> <span><span class="font-bold text-white">Hybrid Assessments:</span> Combine AI-assisted tasks with non-AI tasks.</span></li>
                <li class="flex items-start"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-green-400 mr-4 mt-1 flex-shrink-0"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path><polyline points="22 4 12 14.01 9 11.01"></polyline></svg> <span><span class="font-bold text-white">Classroom AI Policy:</span> Define acceptable use <span class="italic">with</span> students.</span></li>
            </ul>
        </div>

        <!-- NEW Slide 16: Interactive Poll for Policy -->
        <div class="slide text-center">
            <h1 class="text-4xl font-bold text-cyan-400 mb-4">Let's Brainstorm Together</h1>
            <p class="text-xl text-gray-300 mb-8">What is one rule you would put in your classroom's AI usage policy?</p>
            <div class="mt-8 text-center">
                <p class="text-2xl text-gray-300">Go to:</p>
                <a href="https://www.menti.com" target="_blank" class="text-4xl font-bold text-cyan-400 hover:underline">www.menti.com</a>
                <p class="mt-6 text-2xl text-gray-300">And enter the code:</p>
                <p class="mt-2 text-5xl font-bold tracking-widest text-white bg-gray-700/50 px-6 py-3 rounded-lg inline-block">6660 0938</p>
            </div>
        </div>

        <!-- Slide 17: Gemini Group Discussion -->
        <div class="slide text-center">
             <div class="w-full h-full flex flex-col text-center">
                <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-cyan-400 mx-auto mb-4"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
                <h1 class="text-4xl font-bold text-white mb-2">✨ Live Policy Synthesis</h1>
                <p class="text-lg text-gray-300 mb-4">Enter ideas from the discussion below.</p>
                <div class="flex-grow flex flex-col gap-4 min-h-0">
                    <textarea id="demo2-ideas" class="w-full flex-grow p-3 bg-gray-900 rounded-lg text-base text-gray-300 border border-gray-700 focus:ring-2 focus:ring-cyan-500 focus:outline-none" placeholder="e.g., 'Cite the AI', 'Don't copy paste', 'Use it for ideas not answers'..."></textarea>
                    <button id="demo2-button" class="w-full flex justify-center items-center gap-2 px-4 py-2 bg-cyan-600 text-white font-bold rounded-lg hover:bg-cyan-500 transition-colors disabled:bg-gray-600 disabled:cursor-not-allowed">
                        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m5 3 7 5.5L19 3"/><path d="m5 21 7-5.5L19 21"/><path d="M12 22V8"/></svg>
                        <span class="button-text">Synthesize</span>
                    </button>
                    <div class="w-full bg-cyan-900/30 rounded-lg border border-cyan-500/30 p-4 text-left max-h-32 overflow-y-auto">
                        <h3 class="font-bold text-cyan-400 mb-2">Synthesized Policy Rule:</h3>
                        <p id="demo2-output" class="text-white text-base"><span class="text-gray-500">Synthesized rule will appear here.</span></p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Slide 18: Key Takeaways -->
        <div class="slide text-left">
            <h1 class="text-4xl font-bold text-cyan-400 mb-8 text-center">Key Takeaways</h1>
            <ul class="space-y-4 text-xl text-gray-300">
                <li class="flex items-start"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-cyan-400 mr-4 mt-1 flex-shrink-0"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path><polyline points="22 4 12 14.01 9 11.01"></polyline></svg> <span>Vibe coding shifts focus from syntax to <span class="font-bold text-white">systems thinking</span>.</span></li>
                <li class="flex items-start"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-cyan-400 mr-4 mt-1 flex-shrink-0"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path><polyline points="22 4 12 14.01 9 11.01"></polyline></svg> <span>AI is a powerful tool for <span class="font-bold text-white">creation and problem-solving</span>.</span></li>
                <li class="flex items-start"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-cyan-400 mr-4 mt-1 flex-shrink-0"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path><polyline points="22 4 12 14.01 9 11.01"></polyline></svg> <span>We can maintain integrity with <span class="font-bold text-white">new assessment strategies</span>.</span></li>
                <li class="flex items-start"><svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-cyan-400 mr-4 mt-1 flex-shrink-0"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path><polyline points="22 4 12 14.01 9 11.01"></polyline></svg> <span>The goal is to <span class="font-bold text-white">augment, not replace</span>, critical thinking.</span></li>
            </ul>
        </div>

        <!-- Slide 19: Call To Action -->
        <div class="slide text-center">
            <svg xmlns="http://www.w3.org/2000/svg" width="60" height="60" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-cyan-400 mx-auto mb-6"><line x1="22" y1="2" x2="11" y2="13"></line><polygon points="22 2 15 22 11 13 2 9 22 2"></polygon></svg>
            <h1 class="text-4xl font-bold text-white mb-4">Your Call to <span class="bg-clip-text text-transparent bg-gradient-to-br from-white to-gray-400">Ctrl+AI</span></h1>
            <p class="text-2xl text-gray-300 leading-relaxed max-w-2xl mx-auto">Your challenge is not to ban `Ctrl+AI`, but to teach students how to use it <span class="text-cyan-400">wisely, ethically, and creatively.</span></p>
            <p class="mt-8 text-xl text-white">Start a conversation at your school tomorrow.</p>
        </div>

        <!-- Slide 20: Mahalo -->
        <div class="slide text-center">
            <div class="flex items-center justify-center mb-6">
                <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-cyan-400"><path d="M17.7 7.7a2.5 2.5 0 1 0 0-5.5 2.5 2.5 0 0 0 0 5.5z"/><path d="M4 5h16"/><path d="M2 12h10"/><path d="M12 12h10"/><path d="M15.5 15.5A2.5 2.5 0 1 0 13 13a2.5 2.5 0 0 0 2.5 2.5z"/><path d="M22 22H2"/><path d="M7 7H2"/></svg>
            </div>
            <h1 class="text-6xl font-bold text-white">Mahalo!</h1>
            <p class="mt-8 text-lg text-gray-400">Michael Fricano II | K-6 Design and Technology Teacher & EdTech Enthusiast</p>
            <div class="mt-8">
                 <a href="http://www.edtechnocation.com/vibecoding" target="_blank" class="text-2xl font-bold text-cyan-400 hover:underline">http://www.edtechnocation.com/vibecoding</a>
            </div>
            <p class="mt-4 text-gray-400">Slides, Tool Links, & Sample Policies</p>
        </div>

    </div>

    <!-- Navigation controls -->
    <div class="absolute bottom-4 left-1/2 -translate-x-1/2 flex items-center space-x-4 z-20">
        <button id="prev-btn" class="p-2 bg-gray-700/50 backdrop-blur-sm rounded-full hover:bg-cyan-500/50 transition-colors duration-300">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m15 18-6-6 6-6"></path></svg>
        </button>
        <span id="slide-counter" class="text-gray-400 text-sm">1 / 20</span>
        <button id="next-btn" class="p-2 bg-gray-700/50 backdrop-blur-sm rounded-full hover:bg-cyan-500/50 transition-colors duration-300">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"></path></svg>
        </button>
    </div>
    <div class="absolute top-4 right-4 text-xs text-gray-500 z-20">AI in Education Summit - Hawaii</div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // --- State Management ---
            const slides = document.querySelectorAll('.slide');
            const slideCounter = document.getElementById('slide-counter');
            let currentSlide = 0;
            const totalSlides = slides.length;

            // --- Navigation Functions ---
            const showSlide = (index) => {
                slides.forEach((slide, i) => {
                    slide.classList.remove('active', 'animate-fade-in');
                    if (i === index) {
                        slide.classList.add('active');
                        // A small delay to re-trigger animation on slide change
                        setTimeout(() => slide.classList.add('animate-fade-in'), 10);
                    }
                });
                slideCounter.textContent = `${index + 1} / ${totalSlides}`;
            };

            const nextSlide = () => {
                currentSlide = (currentSlide + 1) % totalSlides;
                showSlide(currentSlide);
            };

            const prevSlide = () => {
                currentSlide = (currentSlide - 1 + totalSlides) % totalSlides;
                showSlide(currentSlide);
            };

            // --- Event Listeners ---
            document.getElementById('next-btn').addEventListener('click', nextSlide);
            document.getElementById('prev-btn').addEventListener('click', prevSlide);
            
            document.addEventListener('keydown', (event) => {
                if (event.key === 'ArrowRight') nextSlide();
                if (event.key === 'ArrowLeft') prevSlide();
            });

            // --- Demo 1: Vibe Coding Logic (Pre-written) ---
            const demo1Button = document.getElementById('demo1-button');
            const demo1Output = document.getElementById('demo1-output');
            const prewrittenCode = `import random

# The computer thinks of a secret number between 1 and 20.
secret_number = random.randint(1, 20)

# This variable will store the player's guess.
# We start it at 0 so the game loop can begin.
guess = 0

# Keep playing the game until the player guesses correctly.
while guess != secret_number:
    # Ask the player to guess a number.
    # We use input() to get text from the player.
    guess_text = input("Guess a number between 1 and 20: ")

    # We need to turn the text guess into a whole number (integer)
    # so we can compare it with the secret number.
    guess = int(guess_text)

    # Check if the guess is too low.
    if guess < secret_number:
        print("Your guess is too low. Try again!")
    # Check if the guess is too high.
    elif guess > secret_number:
        print("Your guess is too high. Try again!")
    # If it's not too low and not too high, it must be correct!
    else:
        print("Congratulations! You guessed the secret number!")`;

            demo1Button.addEventListener('click', () => {
                // Set loading state
                demo1Button.disabled = true;
                demo1Button.querySelector('.button-text').textContent = 'Generating...';
                demo1Output.innerHTML = '<span class="text-gray-500">Generating code...</span>';

                // Simulate a delay
                setTimeout(() => {
                    // Reset loading state and show result
                    demo1Output.textContent = prewrittenCode;
                    demo1Button.disabled = false;
                    demo1Button.querySelector('.button-text').textContent = 'Generate Code';
                }, 1000); // 1 second delay
            });

            // --- Interactive Game Logic ---
            const gameMessage = document.getElementById('game-message');
            const gameInput = document.getElementById('game-input');
            const gameGuessBtn = document.getElementById('game-guess-btn');
            const gameResetBtn = document.getElementById('game-reset-btn');
            let secretNumber;

            function newGame() {
                secretNumber = Math.floor(Math.random() * 20) + 1;
                gameMessage.textContent = "I'm thinking of a number between 1 and 20.";
                gameMessage.className = "text-lg text-center h-12 mb-4";
                gameInput.value = "";
                gameInput.disabled = false;
                gameGuessBtn.disabled = false;
            }

            function checkGuess() {
                const guess = parseInt(gameInput.value);
                if (isNaN(guess) || guess < 1 || guess > 20) {
                    gameMessage.textContent = "Please enter a number between 1 and 20.";
                    return;
                }

                if (guess < secretNumber) {
                    gameMessage.textContent = "Your guess is too low! Try again.";
                } else if (guess > secretNumber) {
                    gameMessage.textContent = "Your guess is too high! Try again.";
                } else {
                    gameMessage.textContent = `Congratulations! You got it! The number was ${secretNumber}.`;
                    gameMessage.className = "text-lg text-center h-12 mb-4 text-green-400 font-bold";
                    gameInput.disabled = true;
                    gameGuessBtn.disabled = true;
                }
                gameInput.value = "";
                gameInput.focus();
            }

            gameGuessBtn.addEventListener('click', checkGuess);
            gameInput.addEventListener('keypress', (e) => {
                if (e.key === 'Enter') {
                    checkGuess();
                }
            });
            gameResetBtn.addEventListener('click', newGame);
            
            // --- Demo 2: Policy Synthesis Logic (Pre-written) ---
            const demo2Button = document.getElementById('demo2-button');
            const demo2Output = document.getElementById('demo2-output');
            const prewrittenPolicy = "Use AI as a creative partner for brainstorming and a helpful assistant for debugging, but always write your final code yourself and be prepared to explain how it works.";

            demo2Button.addEventListener('click', () => {
                // Set loading state
                demo2Button.disabled = true;
                demo2Button.querySelector('.button-text').textContent = 'Synthesizing...';
                demo2Output.innerHTML = '<span class="text-gray-500">Synthesizing ideas...</span>';

                // Simulate a delay
                setTimeout(() => {
                    // Reset loading state and show result
                    demo2Output.textContent = prewrittenPolicy;
                    demo2Button.disabled = false;
                    demo2Button.querySelector('.button-text').textContent = 'Synthesize';
                }, 1000); // 1 second delay
            });

            // --- Initial Setup ---
            newGame(); // Initialize the game on load
            showSlide(currentSlide);
        });
    </script>
    
    <!-- Script for Bowling App -->
    <script type="text/babel">
        const BowlingApp = () => {
            const { useState, useEffect } = React;

            const initialFrames = () => Array(10).fill(null).map(() => ({ rolls: [], display: [] }));

            const isStrike = (frame) => frame && frame.rolls[0] === 10;
            const isSpare = (frame) => frame && frame.rolls.length === 2 && frame.rolls[0] + frame.rolls[1] === 10;

            const [frames, setFrames] = useState(initialFrames());
            const [currentFrameIndex, setCurrentFrameIndex] = useState(0);
            const [pinsRemaining, setPinsRemaining] = useState(10);
            const [gameOver, setGameOver] = useState(false);
            const [cumulativeScores, setCumulativeScores] = useState(Array(10).fill(null));

            const calculateScores = (currentFrames) => {
                let runningTotal = 0;
                const newCumulativeScores = Array(10).fill(null);

                for (let i = 0; i < 10; i++) {
                    const frame = currentFrames[i];
                    if (!frame || frame.rolls.length === 0) break;

                    let frameScore = 0;

                    if (i < 9) {
                        if (isStrike(frame)) {
                            const nextFrame = currentFrames[i + 1];
                            const nextNextFrame = currentFrames[i + 2];
                            if (nextFrame && nextFrame.rolls.length > 0) {
                                if (isStrike(nextFrame)) {
                                    if (nextNextFrame && nextNextFrame.rolls.length > 0) {
                                        frameScore = 10 + 10 + nextNextFrame.rolls[0];
                                    } else { break; }
                                } else if (nextFrame.rolls.length === 2) {
                                    frameScore = 10 + nextFrame.rolls[0] + nextFrame.rolls[1];
                                } else { break; }
                            } else { break; }
                        } else if (isSpare(frame)) {
                            const nextFrame = currentFrames[i + 1];
                            if (nextFrame && nextFrame.rolls.length > 0) {
                                frameScore = 10 + nextFrame.rolls[0];
                            } else { break; }
                        } else {
                           if (frame.rolls.length === 2) {
                             frameScore = frame.rolls[0] + frame.rolls[1];
                           } else { break; }
                        }
                    } else {
                        frameScore = frame.rolls.reduce((sum, roll) => sum + roll, 0);
                    }
                    
                    if(frameScore !== null) {
                        runningTotal += frameScore;
                        newCumulativeScores[i] = runningTotal;
                    }
                }
                setCumulativeScores(newCumulativeScores);
            };
            
            useEffect(() => {
                calculateScores(frames);
            }, [frames]);

            const handleRoll = (pins) => {
                if (gameOver) return;

                const newFrames = [...frames];
                let currentFrame = newFrames[currentFrameIndex];
                currentFrame.rolls.push(pins);
                
                if (currentFrameIndex < 9) {
                    if (isStrike({ rolls: [pins] })) {
                        currentFrame.display = ['', 'X'];
                        setCurrentFrameIndex(currentFrameIndex + 1);
                        setPinsRemaining(10);
                    } else if (currentFrame.rolls.length === 1) {
                        setPinsRemaining(10 - pins);
                    } else if (isSpare(currentFrame)) {
                        currentFrame.display = [currentFrame.rolls[0], '/'];
                        setCurrentFrameIndex(currentFrameIndex + 1);
                        setPinsRemaining(10);
                    } else {
                        currentFrame.display = [...currentFrame.rolls];
                        setCurrentFrameIndex(currentFrameIndex + 1);
                        setPinsRemaining(10);
                    }
                } else { // 10th Frame
                    currentFrame.display = currentFrame.rolls.map((roll, idx) => {
                        if (roll === 10) return 'X';
                        if (idx === 1 && currentFrame.rolls[0] + roll === 10) return '/';
                        return roll;
                    });
                    
                    const [r1, r2, r3] = currentFrame.rolls;
                    const isStrike1 = r1 === 10;
                    const isSpare1 = r1 + r2 === 10;

                    if (currentFrame.rolls.length === 3 || (currentFrame.rolls.length === 2 && !isStrike1 && !isSpare1)) {
                        setGameOver(true);
                    } else {
                        if (isStrike1 || isSpare1) {
                            setPinsRemaining(10 - (pins === 10 ? 0 : pins));
                             if (isStrike1 && r2 !== 10) setPinsRemaining(10 - r2);
                        } else {
                            setPinsRemaining(10 - pins);
                        }
                    }
                }
                setFrames(newFrames);
            };
            
            const newGame = () => {
                setFrames(initialFrames());
                setCurrentFrameIndex(0);
                setPinsRemaining(10);
                setGameOver(false);
                setCumulativeScores(Array(10).fill(null));
            };

            const Scorecard = ({ frames, cumulativeScores }) => (
                <div className="bg-gray-800 rounded-lg shadow-2xl p-2 md:p-4">
                    <div className="grid grid-cols-10 gap-1">
                        {frames.map((frame, index) => (
                            <Frame key={index} frameIndex={index} frame={frame} cumulativeScore={cumulativeScores[index]} />
                        ))}
                    </div>
                </div>
            );

            const Frame = ({ frameIndex, frame, cumulativeScore }) => {
                const isTenth = frameIndex === 9;
                const display = (frame && frame.display) || [];

                return (
                    <div className="flex flex-col border border-gray-600 rounded-md text-xs md:text-base">
                        <div className="text-center bg-gray-700 font-semibold py-1 rounded-t-md">{frameIndex + 1}</div>
                        <div className="flex-grow flex h-8 md:h-10">
                            <div className={`w-1/2 border-r ${isTenth ? '' : ''} border-gray-600 flex items-center justify-center`}>
                                {display[0] !== undefined ? display[0] : ''}
                            </div>
                            <div className={`w-1/2 ${isTenth ? 'border-r' : ''} border-gray-600 flex items-center justify-center`}>
                                {display[1] !== undefined ? display[1] : ''}
                            </div>
                            {isTenth && (
                                <div className="w-1/2 flex items-center justify-center">
                                    {display[2] !== undefined ? display[2] : ''}
                                </div>
                            )}
                        </div>
                        <div className="text-center font-bold text-sm md:text-xl py-1 md:py-2 h-8 md:h-10 flex items-center justify-center">
                            {cumulativeScore}
                        </div>
                    </div>
                );
            };

            const Controls = ({ onRoll, pinsRemaining, gameOver, onNewGame }) => (
                <div className="bg-gray-800 rounded-lg shadow-2xl p-2 md:p-4 mt-4">
                    <div className="grid grid-cols-6 gap-1 md:gap-2 mb-4">
                        {Array.from({ length: 11 }, (_, i) => i).map(pins => (
                            <button
                                key={pins}
                                onClick={() => onRoll(pins)}
                                disabled={pins > pinsRemaining || gameOver}
                                className="bg-cyan-600 text-white font-bold py-2 md:py-3 rounded-lg shadow-md hover:bg-cyan-500 disabled:bg-gray-600 disabled:opacity-50 disabled:cursor-not-allowed transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-cyan-400"
                            >
                                {pins}
                            </button>
                        ))}
                    </div>
                    <button
                        onClick={onNewGame}
                        className="w-full bg-green-600 text-white font-bold py-2 md:py-3 rounded-lg shadow-md hover:bg-green-500 transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-green-400"
                    >
                        New Game
                    </button>
                </div>
            );

            return (
                <div className="w-full max-w-4xl mx-auto">
                    <header className="text-center mb-2 md:mb-4">
                        <h1 className="text-2xl md:text-3xl font-bold text-cyan-400 tracking-wider">Bowling Score Tracker</h1>
                    </header>
                    <Scorecard frames={frames} cumulativeScores={cumulativeScores} />
                    <div className="text-center my-2 md:my-4">
                        {gameOver ? (
                            <p className="text-lg md:text-xl font-bold text-green-400 animate-pulse">Game Over! Final Score: {cumulativeScores[9]}</p>
                        ) : (
                             <p className="text-base md:text-lg">Frame {currentFrameIndex + 1} - Pins Remaining: <span className="font-bold text-yellow-400">{pinsRemaining}</span></p>
                        )}
                    </div>
                    <Controls onRoll={handleRoll} pinsRemaining={pinsRemaining} gameOver={gameOver} onNewGame={newGame} />
                </div>
            );
        };

        ReactDOM.render(<BowlingApp />, document.getElementById('bowling-app-root'));
    </script>
</body>
</html>
